<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6 px-md-0">
      <h2 class="text-center mt-2" style="font-family: HiraMinPro-W6;">ログイン</h2>
    <div class="text-danger">
      <%= flash[:alert] %>
    </div>
      <span class="my-3">会員の方はこちらからログイン</span>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="row form-group mt-3">
         <%= f.label :"メールアドレス", class:"col-md-6 col-form-label text-md-right fw-bold" %>
      <div class="col-md-6 px-md-0">
        <%= f.email_field :email, autofocus: true, placeholder: "sample@example.com", class:"form-control", autocomplete: "off"  %>
      </div>
      </div>

      <div class="row form-group mt-3">
         <%= f.label :"パスワード", class:"col-md-6 col-form-label text-md-right fw-bold" %>
      <div class="col-md-6 px-md-0">
        <%= f.password_field :password, autofocus: true, placeholder: "パスワードを入力してください", class:"form-control" %>
      </div>
      </div>

      <div class="mt-2 login-buuton">
        <%= f.submit "ログイン", class: "btn btn-md btn-primary"%>
      </div>
      <% end %>
      <div>
        <span>会員登録がお済みでない⽅</span>
        <p><%= link_to "こちら", new_customer_registration_path %> から新規登録を行ってください。</p>
      </div>
    </div>
  </div>
</div>